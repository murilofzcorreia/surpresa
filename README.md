# 💜 Ana Paula

Página especial feita com amor.

## Como subir no GitHub Pages

1. Crie um repositório no GitHub (ex: `ana-paula`)
2. Faça upload dos arquivos ou clone e adicione:
   ```bash
   git init
   git add .
   git commit -m "primeira versão"
   git remote add origin https://github.com/SEU_USUARIO/SEU_REPO.git
   git push -u origin main
   ```
3. Vá em **Settings → Pages**
4. Em **Source**, selecione `main` e pasta `/` (root)
5. Clique **Save** — em alguns minutos a URL estará no ar!

## Estrutura

```
/
└── index.html   ← página principal (tudo incluso)
└── README.md
```

